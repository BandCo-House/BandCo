const { writeJson } = require('./codex-hook-utils');

writeJson({
  systemMessage: 'JamPlay backend harness is active. Use AGENTS.md as the source of truth, use pnpm only, and verify changes before completion.',
});

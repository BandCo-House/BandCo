/**
 * 멤버·팀원 선택 UI가 한 번에 불러오는 인원 수.
 *
 * 백엔드의 목록 API는 take 기본값이 20이라, 안 주면 21번째 멤버부터 조용히 빠진다.
 * 선택 화면은 커서 페이지네이션을 쓰지 않고 한 번에 다 보여주므로 넉넉히 잡는다.
 * 밴드·팀 규모가 이 수를 넘기 시작하면 그때는 페이지네이션을 붙여야 한다.
 */
export const MEMBER_PICKER_TAKE = 100;
